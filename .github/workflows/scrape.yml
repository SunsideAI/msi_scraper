name: MSI Scraper

on:
  schedule:
    - cron: "0 7 * * *"   # täglich 07:00 UTC (09:00 DE)
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install Deps
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 lxml openai

      - name: Run MSI Scraper (auto)
        env:
          AIRTABLE_TOKEN: ${{ secrets.AIRTABLE_TOKEN }}
          AIRTABLE_BASE: ${{ secrets.AIRTABLE_BASE }}
          AIRTABLE_TABLE_ID: ${{ secrets.AIRTABLE_TABLE_ID }}
          AIRTABLE_VIEW: ${{ secrets.AIRTABLE_VIEW }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python msi_v2_airtable_replace.py auto

      - name: Upload CSV Artefakte
        uses: actions/upload-artifact@v4
        with:
          name: scraped-data
          path: |
            msi_kauf.csv
            msi_miete.csv
